function test() {
    console.log("sup")
    var lijst, txtValue

    // txtValue = dict.textContent || dict.innerText
    // console.log(txtValue)
    // lijst = txtValue.split(",")
    // console.log(lijst)
}